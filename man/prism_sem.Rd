% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prism_sem.R
\name{prism_sem}
\alias{prism_sem}
\title{SEM Covariance Projection (PRISM v2)}
\usage{
prism_sem(
  data,
  model,
  initial_imputation = NULL,
  lambda_sigma = NULL,
  lr = 0.01,
  tol_kkT = 1e-04,
  tol_cov = 1e-06,
  max_iter = 2000
)
}
\arguments{
\item{data}{A data frame containing missing values.}

\item{model}{Either lavaan model syntax (a single string) or a fitted
lavaan object. When syntax is supplied it is fit with
\code{lavaan::sem(model, data = data, missing = "fiml")}. When a fitted
object is supplied it is used as-is and should have been estimated with
\code{missing = "fiml"}.}

\item{initial_imputation}{A data frame or matrix of the same dimensions as
the observed variables of the model, containing initial imputed values.
If \code{NULL} (default), column-mean imputation is used as a fallback.}

\item{lambda_sigma}{A non-negative numeric value giving the structural
weight of the covariance-matching term relative to the fidelity term.
\code{0} returns the initial imputation; larger values enforce the
model-implied structure more strongly. Because the covariance term
carries a \eqn{1/(n-1)} scaling, the default is \code{NULL}, which
auto-scales to \code{nrow(data) / 2}; pass an explicit number to
override.}

\item{lr}{A numeric value for the initial gradient descent step size; the
Armijo backtracking line search adapts it automatically. Defaults to 0.01.}

\item{tol_kkT}{A numeric value for the stationarity tolerance on the
projected-gradient (KKT residual) norm. Defaults to 1e-4.}

\item{tol_cov}{A numeric value for the feasibility tolerance on the
covariance gap to the target. Defaults to 1e-6.}

\item{max_iter}{An integer specifying the maximum number of iterations
for the gradient descent projection. Defaults to 2000.}
}
\value{
A data frame with the missing cells completed. Only the
originally-missing cells are modified. The \code{prism_diagnostics}
attribute contains the optimization diagnostics.
}
\description{
Fit a structural equation model via FIML (or reuse an already
fitted model), extract the model-implied covariance matrix, and project
an initial imputation onto that structural target under the PRISM v2
regularized objective. The engine, objective, mean preservation, and KKT
diagnostics are identical to \code{\link{prism_fiml}}; this entry point
supports any single-group SEM estimated by \code{lavaan::sem()} —
CFAs, structural regressions among latent variables, mediation models,
and models with covariates. Only originally-missing cells are modified;
observed data are held fixed.
}
\details{
The completed data solve the same regularized projection as
\code{\link{prism_fiml}}: fidelity to the initial imputation is traded off
against covariance matching through \code{lambda_sigma}, and the column
means of the initial imputation are preserved exactly by mean-preserving
projected gradients. Convergence is certified by the KKT diagnostics in
the \code{prism_diagnostics} attribute (see \code{\link{prism_fiml}} for
the field descriptions).

Restrictions: the model must be single-group and single-level with
continuous observed variables. Ordinal, multi-group, and multilevel models
are rejected with an error. As in \code{prism_fiml}, model-implied means
are deliberately not targeted; first moments are anchored to the initial
imputation.
}
\examples{
\dontrun{
library(lavaan)
model <- "
  ind60 =~ x1 + x2 + x3
  dem60 =~ y1 + y2 + y3 + y4
  dem65 =~ y5 + y6 + y7 + y8
  dem60 ~ ind60
  dem65 ~ ind60 + dem60
"
df <- lavaan::PoliticalDemocracy
df$x1[1:10] <- NA
result <- prism_sem(df, model)
attr(result, "prism_diagnostics")
}
}
